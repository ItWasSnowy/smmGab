@model List<SmmGab.Domain.Models.Project>
@{
    ViewData["Title"] = "Выбор проекта";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="mb-4">
                <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-outline-secondary">
                        <i class="bi bi-box-arrow-right me-1"></i>Выйти
                    </button>
                </form>
            </div>
            <div class="text-center mb-5">
                <h1 class="display-4 fw-bold mb-3" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                    Выберите проект
                </h1>
                <p class="lead text-muted">Выберите проект для работы или создайте новый</p>
            </div>

            @if (Model.Any())
            {
                <div class="row g-4 mb-4">
                    @foreach (var project in Model)
                    {
                        <div class="col-md-6">
                            <div class="card h-100 shadow-sm border-0 project-card">
                                <div class="card-body d-flex flex-column">
                                    <div class="d-flex align-items-center mb-3">
                                        <div class="project-icon bg-primary bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 60px; height: 60px;">
                                            <i class="bi bi-folder text-white fs-3"></i>
                                        </div>
                                        <div class="flex-grow-1">
                                            <h5 class="card-title mb-1 fw-bold">@project.Name</h5>
                                            <div class="d-flex align-items-center text-muted small">
                                                <i class="bi bi-calendar3 me-1"></i>
                                                <span>@project.CreatedAtUtc.ToString("dd.MM.yyyy")</span>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="mt-auto">
                                        <form asp-action="SelectProject" method="post" class="d-inline">
                                            <input type="hidden" name="projectId" value="@project.Id" />
                                            <button type="submit" class="btn btn-primary w-100">
                                                <i class="bi bi-box-arrow-in-right me-2"></i>Открыть проект
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <div class="mb-4">
                        <i class="bi bi-folder-x display-1 text-muted"></i>
                    </div>
                    <h3 class="text-muted mb-3">У вас пока нет проектов</h3>
                    <p class="text-muted mb-4">Создайте первый проект, чтобы начать работу</p>
                </div>
            }

            <div class="text-center">
                <a asp-controller="Projects" asp-action="Create" class="btn btn-lg btn-primary" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border: none;">
                    <i class="bi bi-plus-circle me-2"></i>Создать новый проект
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    .project-card {
        transition: transform 0.2s, box-shadow 0.2s;
        border: 1px solid #e9ecef !important;
    }

    .project-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important;
    }

    .project-icon {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
</style>

