# SMM Planner

Современная система планирования и публикации контента в социальных сетях (VK, Telegram).

## Возможности

- 📁 **Управление проектами** - создавайте и управляйте проектами
- 📡 **Каналы** - подключение каналов VK и Telegram
- 📝 **Публикации** - создание и планирование публикаций с поддержкой форматированного текста
- 📅 **Календарь** - визуализация запланированных публикаций
- ⏰ **Автоматическая публикация** - фоновый сервис для автоматической публикации по расписанию
- 🎨 **Современный дизайн** - красивый и интуитивный интерфейс

## Технологии

- **Backend**: ASP.NET Core 8.0 (C#)
- **Frontend**: MVC с Bootstrap 5
- **База данных**: PostgreSQL
- **ORM**: Entity Framework Core
- **Аутентификация**: ASP.NET Core Identity

## Требования

- .NET 8.0 SDK
- PostgreSQL 12+
- Visual Studio 2022 или VS Code (рекомендуется)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd SmmGab
```

2. Настройте строку подключения к базе данных в `appsettings.json`:
```json
{
  "ConnectionStrings": {
    "Default": "Host=localhost;Database=smmplanner;Username=postgres;Password=your_password"
  }
}
```

3. Примените миграции:
```bash
cd SmmGab
dotnet ef migrations add InitialCreate
dotnet ef database update
```

4. Запустите приложение:
```bash
dotnet run
```

5. Откройте браузер и перейдите по адресу `https://localhost:7193`

## Настройка

### VK API

1. Создайте приложение на [VK Developers](https://vk.com/dev)
2. Получите токен доступа с правами: `wall`, `photos`, `groups`
3. При создании канала VK укажите:
   - **External ID**: ID группы (отрицательное число, например `-123456789`)
   - **Auth Ref**: JSON с токеном `{"token": "your_vk_token"}`

### Telegram Bot API

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Добавьте бота в канал/группу как администратора
4. При создании канала Telegram укажите:
   - **External ID**: ID канала/группы (число или username с @)
   - **Auth Ref**: Токен бота (строка)

## Структура проекта

```
SmmGab/
├── Controllers/          # MVC контроллеры
├── Views/               # Razor представления
├── Domain/              # Доменные модели
├── Data/                # Контекст базы данных
├── Application/         # Интерфейсы приложения
├── Infrastructure/      # Реализация сервисов
│   ├── Connectors/     # Публикаторы (VK, Telegram)
│   └── Services/        # Сервисы (файлы, Delta Quill)
└── Background/          # Фоновые сервисы
```

## API

Приложение также предоставляет REST API для интеграции:

- `GET /api/projects` - список проектов
- `GET /api/channels` - список каналов
- `GET /api/publications` - список публикаций
- `POST /api/publications/{id}/publish-now` - немедленная публикация

Полная документация API доступна по адресу `/swagger` в режиме разработки.

## Лицензия

MIT License

## Автор

SMM Planner Team

